// "new" - add new todo FileList
// "list" - show all list
// "delete" - delete on demand
// "quit" - quit app


let input = prompt("Enter new to add list");

while (input) {
    if (input = "new") {

    }
}